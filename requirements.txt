web3==8.0.0
python-dotenv==1.2.3
sibyl-memory-client==0.8.0
sibyl-memory-cli[mcp]==0.4.0

# Optional: only needed to wire in the off-path Virtuals ACP seam (VirtualsCoordinator).
# requests>=2.31.0

# Optional: only needed to render the dashboard to a PNG (tools/shoot_dashboard.py).
# The `dashboard` command itself needs nothing beyond the core deps.
# Pillow>=10.0.0
